# API framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0

# Workflow orchestration
langgraph>=0.2,<2.0

# Validation & config
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0

# Deterministic project-scaffold templating (app/services/boilerplate.py)
jinja2>=3.1,<4.0

# LLM provider (Claude)
anthropic>=0.40,<1.0

# exec-sandbox MCP client (app/integrations/executor.py :: MCPExecutor, repair-tool binding)
langchain-core>=1.0,<2.0
langchain-mcp-adapters>=0.3,<1.0

# Testing
pytest>=8.3,<10.0
httpx>=0.27,<1.0

# Smoke check (B3): static analysis of generated Python — undefined names / bad imports / syntax.
# Pure-Python, zero transitive deps. The smoke check degrades gracefully if it's not installed.
pyflakes>=3.0,<4.0
