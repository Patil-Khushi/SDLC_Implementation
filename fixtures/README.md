# fixtures/

Design-package artifacts used as **test fixtures** for the code-generation pipeline — not
production documentation.

## tic-tac-toe/

A complete, ready-to-generate design package (`SKILL.md`, `design-tokens.json`,
`functional-html-mockup.html`, `schema.sql`, etc.) demonstrating the full contract Team 2's
Design Package hands to Team 3 (`contracts/design-to-implementation/`). It's used for manual
pipeline smoke-testing end-to-end — scaffold through code generation — against a realistic,
non-trivial input instead of a synthetic one-file stub.

Do not treat anything under this directory as a real product spec.
