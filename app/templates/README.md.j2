# {{ project.name }}

{{ project.description }}
{% if backend.enabled %}

## Backend

{{ backend.framework | capitalize }} service.

    pip install -r requirements.txt
{% if backend.framework == "flask" %}
    flask --app app.main run --debug
{% else %}
    uvicorn app.main:app --reload
{% endif %}
{% endif %}
{% if frontend.enabled %}

## Frontend

{{ frontend.framework | capitalize }} + TypeScript app (bundled with {{ frontend.bundler }}).

    npm install
{% for name in frontend.scripts %}
    npm run {{ name }}
{% endfor %}
{% endif %}
{% if docker.enabled %}

## Docker

    docker compose up --build
{% endif %}
