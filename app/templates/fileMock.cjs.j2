// Static-asset stub for Jest — DETERMINISTIC scaffold output (app/services/boilerplate.py).
//
// Bundlers turn `import logo from './logo.svg'` into a URL string; Jest has no such loader and
// would hand the raw file to the JS parser. jest.config.cjs maps every image/icon extension here,
// so asset imports resolve to a stable, assertable placeholder instead of a parse error.
module.exports = 'test-file-stub';
