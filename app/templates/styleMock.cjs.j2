// Stylesheet stub for Jest — DETERMINISTIC scaffold output (app/services/boilerplate.py).
//
// An OBJECT, not a string: it has to serve both `import './App.css'` (side-effect import, value
// unused) and `import styles from './App.module.css'` (CSS-module access — `styles.button` must
// read as undefined rather than throw). jest.config.cjs maps css/less/sass/scss here.
module.exports = {};
