# {{ project.name }} — copy to .env and fill in real values
{% for var in environment.variables %}
{{ var }}=
{% endfor %}
{% if not environment.variables %}
# No environment variables are required for the selected capabilities.
{% endif %}
